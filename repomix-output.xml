This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
cloudfunctions/
  addSentenceToBook/
    config.json
    index.js
    package.json
  checkFieldExists/
    config.json
    index.js
    package.json
  createBook/
    config.json
    index.js
    package.json
  createSentence/
    config.json
    index.js
    package.json
  edgeTts/
    config.json
    dbHelper.js
    index.js
    package.json
  ensureUserBook/
    config.json
    index.js
    package.json
  getArticles/
    config.json
    index.js
    package.json
  getArticleSentences/
    config.json
    dbHelper.js
    index.js
    package.json
  getBooks/
    config.json
    index.js
    package.json
  getBookSentence/
    config.json
    index.js
    package.json
  getFavoriteSentences/
    config.json
    index.js
    package.json
  getOpenId/
    config.json
    index.js
    package.json
  getSentencePlayList/
    config.json
    index.js
    package.json
  renameField/
    config.json
    index.js
    package.json
  searchSentences/
    config.json
    index.js
    package.json
  translate/
    config.json
    index.js
    package.json
  upsertMark/
    config.json
    index.js
    package.json
miniprogram/
  enums/
    app-enums.ts
  pages/
    index/
      index.json
      index.ts
      index.wxml
      index.wxss
  services/
    article-service.ts
    book-service.ts
    cloud-client.ts
    sentence-service.ts
    translate-service.ts
    tts-service.ts
  utils/
    util.ts
  app.json
  app.ts
  app.wxss
package.json
project.config.json
project.private.config.json
tsconfig.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="cloudfunctions/addSentenceToBook/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/addSentenceToBook/index.js">
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

/**
 * 把一个已存在的句子（可能是系统例句，也可能是别的词书里的句子）
 * "加入"到当前用户自己的某本词书。
 *
 * 关键设计：不复制句子内容，只在 sentenceBookRef 表里建一条引用关系。
 * 这样：
 * - 原句子的文本/音频只有一份，不会因为被多个词书引用而冗余；
 * - 学习进度/收藏状态挂在 sentenceMark 上（openid + sentenceId），
 *   跟句子在哪本词书里出现无关，天然支持"同一句话出现在多本词书"。
 *
 * _id 用 `${bookId}__${sentenceId}` 做主键，重复加入会被 set() 自动去重覆盖，
 * 不会插出多条重复引用。
 */
exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext()
  const { sentenceId, bookId } = event || {}

  if (!sentenceId || !bookId) {
    return { code: 400, msg: 'sentenceId / bookId 不能为空' }
  }

  try {
    // 1. 校验目标词书存在，且属于当前用户
    //    （不允许往别人的词书、或公共词书里随便塞句子）
    const bookDoc = await db.collection('book').doc(bookId).get().catch(() => null)
    const book = bookDoc && bookDoc.data
    if (!book) {
      return { code: 404, msg: '词书不存在' }
    }
    if (book.openid && book.openid !== OPENID) {
      return { code: 403, msg: '无权操作该词书' }
    }

    // 2. 校验句子存在
    const sentenceDoc = await db.collection('sentence').doc(sentenceId).get().catch(() => null)
    if (!sentenceDoc || !sentenceDoc.data) {
      return { code: 404, msg: '句子不存在' }
    }

    // 3. 如果句子本来就直接归属这本词书，不需要再建引用
    if (sentenceDoc.data.bookId === bookId) {
      return { code: 0, msg: 'success', data: { refId: '', alreadyIn: true } }
    }

    const refId = `${bookId}__${sentenceId}`
    await db.collection('sentenceBookRef').doc(refId).set({
      data: {
        bookId,
        sentenceId,
        openid: OPENID,
        addedAt: Date.now()
      }
    })

    return { code: 0, msg: 'success', data: { refId } }
  } catch (err) {
    console.error('[addSentenceToBook] 加入词书失败:', err)
    return { code: 500, msg: '加入词书失败', error: err }
  }
}
</file>

<file path="cloudfunctions/addSentenceToBook/package.json">
{
  "name": "addSentenceToBook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~4.0.2"
  }
}
</file>

<file path="cloudfunctions/checkFieldExists/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/checkFieldExists/index.js">
const cloud = require('wx-server-sdk');

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
});

const db = cloud.database();
const _ = db.command;

exports.main = async (event, context) => {
  const { collectionName, fieldName } = event;

  // 参数校验
  if (!collectionName || !fieldName) {
    return {
      success: false,
      errorMessage: '缺少必要参数：collectionName 或 fieldName'
    };
  }

  try {
    // 1. 统计总记录数
    const totalCountRes = await db.collection(collectionName).count();
    const totalCount = totalCountRes.total;

    // 2. 统计【存在】该字段的记录数
    const existsCountRes = await db.collection(collectionName)
      .where({
        [fieldName]: _.exists(true)
      })
      .count();
    const existsCount = existsCountRes.total;

    // 3. 计算【不存在】该字段的记录数
    const notExistsCount = totalCount - existsCount;

    return {
      success: true,
      data: {
        collectionName,
        fieldName,
        totalCount,        // 总记录数
        existsCount,       // 包含该字段的记录数
        notExistsCount,    // 不包含该字段的记录数
        existsRatio: totalCount > 0 ? `${((existsCount / totalCount) * 100).toFixed(2)}%` : '0%' // 占比
      }
    };
  } catch (err) {
    console.error('统计字段失败：', err);
    return {
      success: false,
      error: err
    };
  }
};
</file>

<file path="cloudfunctions/checkFieldExists/package.json">
{
  "name": "checkFieldExists",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~4.0.2"
  }
}
</file>

<file path="cloudfunctions/createBook/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/createBook/index.js">
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

/**
 * 创建词书（服务端统一处理，替代原来客户端直接写 db.collection('books') 的方式）
 */
exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext()
  const { name, type = 'sentence' } = event || {}

  const bookName = (name || '').trim()
  if (!bookName) {
    return { code: 400, msg: '词书名称不能为空' }
  }

  try {
    // 避免用户手抖重复点击 / 网络重试导致创建出多本同名词书
    const existed = await db.collection('book')
      .where({ openid: OPENID, name: bookName, type })
      .limit(1)
      .get()

    if (existed.data.length > 0) {
      return { code: 0, msg: 'success', data: existed.data[0] }
    }

    const now = Date.now()
    const addRes = await db.collection('book').add({
      data: {
        name: bookName,
        type,
        openid: OPENID,
        createdAt: now
      }
    })

    return {
      code: 0,
      msg: 'success',
      data: {
        _id: addRes._id,
        name: bookName,
        type,
        openid: OPENID,
        createdAt: now
      }
    }
  } catch (err) {
    console.error('[createBook] 创建词书失败:', err)
    return { code: 500, msg: '创建词书失败', error: err }
  }
}
</file>

<file path="cloudfunctions/createBook/package.json">
{
  "name": "createBook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~4.0.2"
  }
}
</file>

<file path="cloudfunctions/createSentence/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/createSentence/index.js">
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

/**
 * 录入一条新句子。
 *
 * 规则（对应产品侧的约定）：
 * - 不管用户在录入页选的是哪本词书，句子的物理内容永远存进
 *   当前用户的默认库 `origin_${openid}`（"用户句子"）。
 * - 如果选的不是默认库本身，再额外建一条 sentenceBookRef 引用，
 *   让这句话"看起来"出现在用户选的那本词书里。
 * - 这两步放在同一个事务里：要么都成功，要么都不成功，
 *   不会出现"句子存进去了，但引用没建上，用户在目标词书里看不到"的半吊子状态。
 *
 * 权限：目标词书必须是当前用户自己的（openid 匹配），
 * 系统/公共词书（openid 为空）不允许作为录入目标。
 */
exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext()
  const { bookId, en, zh, audio, audio_zh } = event || {}

  if (!bookId) {
    return { code: 400, msg: 'bookId 不能为空' }
  }
  if (!en || !zh) {
    return { code: 400, msg: '中英文内容不能为空' }
  }

  if (!OPENID) {
    return { code: 400, msg: 'openid 不能为空' }
  }

  const originBookId = `origin_${OPENID}`

  // 校验目标词书确实是当前用户自己的，且已初始化过默认库
  if (bookId !== originBookId) {
    const bookDoc = await db.collection('book').doc(bookId).get().catch(() => null)
    const book = bookDoc && bookDoc.data
    if (!book || book.openid !== OPENID) {
      return { code: 403, msg: '无权在该词书中录入句子' }
    }
  }

  const transaction = await db.startTransaction()

  try {
    const now = Date.now()

    const addRes = await transaction.collection('sentence').add({
      data: {
        bookId: originBookId, // 物理归属永远是"用户句子"
        en,
        zh,
        audio: audio || '',
        audio_zh: audio_zh || '',
        createdAt: now
      }
    })

    const sentenceId = addRes._id

    // 录入的目标不是默认库本身时，补一条引用，让句子出现在用户选的那本词书里
    if (bookId !== originBookId) {
      const refId = `${bookId}__${sentenceId}`
      await transaction.collection('sentenceBookRef').add({
        data: {
          _id: refId,
          bookId,
          sentenceId,
          openid: OPENID,
          addedAt: now
        }
      })
    }

    await transaction.commit()

    return { code: 0, msg: 'success', data: { sentenceId } }
  } catch (err) {
    await transaction.rollback()
    console.error('[createSentence] 保存句子失败:', err)
    return { code: 500, msg: '保存失败，请重试', error: err }
  }
}
</file>

<file path="cloudfunctions/createSentence/package.json">
{
  "name": "createSentence",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/edgeTts/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/edgeTts/dbHelper.js">
/**
 * 突破 100 条限制的分批拉取工具函数
 * @param {DB.Collection | DB.Query} query 数据库查询对象
 * @param {number} batchSize 每批次读取数量，默认 100
 * @returns {Promise<Array>} 合并后的完整数组
 */
async function fetchAll(query, batchSize = 100) {
  const countResult = await query.count();
  const total = countResult.total;

  if (total === 0) {
    return [];
  }

  const batchTimes = Math.ceil(total / batchSize);
  const tasks = [];

  for (let i = 0; i < batchTimes; i++) {
    const promise = query.skip(i * batchSize).limit(batchSize).get();
    tasks.push(promise);
  }

  const resArray = await Promise.all(tasks);
  return resArray.reduce((acc, cur) => acc.concat(cur.data), []);
}

/**
 * 统一成功响应
 */
function success(data, message = 'ok') {
  return { code: 0, data, message };
}

/**
 * 统一失败响应
 */
function fail(message = 'error', code = -1) {
  return { code, data: null, message };
}

module.exports = {
  fetchAll,
  success,
  fail
};
</file>

<file path="cloudfunctions/edgeTts/index.js">
const cloud = require('wx-server-sdk')
const { Blob } = require('buffer')
global.Blob = Blob

const { EdgeTTS } = require('edge-tts-universal')
const { success, fail } = require('./dbhelper')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

function getVoice(text) {
  // 包含中文
  if (/[\u4e00-\u9fff]/.test(text)) {
    return 'zh-CN-XiaoxiaoNeural'
  }
  // 默认英语
  return 'en-US-JennyNeural'
}

exports.main = async (event) => {
  const text = event.text

  if (!text) {
    return fail('text 不能为空', 400)
  }

  try {
    const voice = getVoice(text)
    const tts = new EdgeTTS(text, voice)

    const result = await tts.synthesize()

    const audioBuffer = Buffer.from(
      await result.audio.arrayBuffer()
    )

    // 转换逐词时间戳（100纳秒 tick -> 秒）
    const wordBoundaries = (result.subtitle || []).map(w => ({
      text: w.text,
      start: w.offset / 10000000,
      duration: w.duration / 10000000
    }))

    // 直接使用 Buffer 上传到云存储，免去读写 /tmp 磁盘文件
    const cloudPath = `tts/${Date.now()}_${Math.random().toString(36).slice(-6)}.mp3`
    const uploadResult = await cloud.uploadFile({
      cloudPath,
      fileContent: audioBuffer
    })

    return success({
      fileID: uploadResult.fileID,
      wordBoundaries
    }, '语音合成成功')

  } catch (err) {
    console.error('[edgeTts Error]:', err)
    return fail(err.message || '语音合成失败', 500)
  }
}
</file>

<file path="cloudfunctions/edgeTts/package.json">
{
  "name": "edgeTts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest",
    "edge-tts-universal": "^1.4.0"
  }
}
</file>

<file path="cloudfunctions/ensureUserBook/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/ensureUserBook/index.js">
//程序初始化时创建两本用户默认书籍 我的句子和我的收藏
const cloud = require('wx-server-sdk');

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
});

const db = cloud.database();

const DEFAULT_BOOKS = [
  {
    role: 'origin',
    prefix: 'origin',
    name: '我的录入',
    type: 'sentence',
  },
  {
    role: 'favorite',
    prefix: 'fav',
    name: '我的收藏',
    type: 'sentence',
  }
];

// 2. 替换 export 为 exports.main
exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext();
  const now = Date.now();

  try {
    const bookPromises = DEFAULT_BOOKS.map(async (config) => {
      const bookId = `${config.prefix}_${OPENID}`;
      const bookRef = db.collection('book').doc(bookId);

      const { data } = await bookRef.get().catch(() => ({ data: null }));

      if (data) {
        return data;
      }

      const newBook = {
        name: config.name,
        type: config.type,
        role: config.role,
        openid: OPENID,
        itemCount: 0,
        createdAt: now,
        updatedAt: now,
      };

      await bookRef.set({
        data: newBook
      });

      return newBook;
    });

    const resultBooks = await Promise.all(bookPromises);

    const originBook = resultBooks.find(b => b.role === 'origin');
    const favoriteBook = resultBooks.find(b => b.role === 'favorite');

    return {
      code: 0,
      msg: 'User default books ensured successfully',
      data: {
        originBook,
        favoriteBook,
        originBookId: originBook?._id,
        favoriteBookId: favoriteBook?._id,
      }
    };

  } catch (err) {
    console.error('[ensureUserBook Error]:', err);
    return {
      code: -1,
      msg: 'Failed to ensure user default books',
      error: err
    };
  }
};
</file>

<file path="cloudfunctions/ensureUserBook/package.json">
{
  "name": "ensureUserBook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/getArticles/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/getArticles/index.js">
const cloud = require('wx-server-sdk');

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

// 定义常量
const MAX_LIMIT = 100;

exports.main = async (event, context) => {
  try {
    const { bookId } = event;

    // 1. 构建查询条件
    const whereCondition = bookId ? { bookId } : {};

    // 2. 调用修正后的全量拉取函数
    const list = await fetchAll('article', whereCondition);

    // 3. 提取需要换取临时 URL 的 fileID
    const fileList = list
      .map(item => item.audioUrl)
      .filter(Boolean);

    // 4. 分批并发获取音频临时 URL（每次最多 50 个）
    const urlMap = new Map();
    if (fileList.length > 0) {
      const batchPromises = [];
      for (let i = 0; i < fileList.length; i += 50) {
        const batch = fileList.slice(i, i + 50);
        batchPromises.push(cloud.getTempFileURL({ fileList: batch }));
      }

      // 使用 Promise.all 并发请求，大幅提升耗时性能
      const batchResults = await Promise.all(batchPromises);
      batchResults.forEach(res => {
        (res.fileList || []).forEach(item => {
          if (item.tempFileURL) {
            urlMap.set(item.fileID, item.tempFileURL);
          }
        });
      });
    }

    // 5. 替换字段中的临时 URL
    list.forEach(item => {
      if (item.audioUrl) {
        item.audioUrl = urlMap.get(item.audioUrl) || '';
      }
    });

    return {
      code: 0,
      msg: 'success',
      data: list
    };

  } catch (err) {
    console.error('[getArticles] 失败:', err);
    return {
      code: -1,
      msg: err.message || '获取文章列表失败',
      data: null
    };
  }
};

/**
 * 突破 100 条限制，全量并发拉取数据库记录
 * @param {string} collectionName 集合名称
 * @param {object} whereCondition 查询条件
 */
async function fetchAll(collectionName, whereCondition = {}) {
  // 1. 获取总数
  const countRes = await db.collection(collectionName).where(whereCondition).count();
  const total = countRes.total;
  if (total === 0) return [];

  // 2. 计算分批次数
  const batchTimes = Math.ceil(total / MAX_LIMIT);
  const tasks = [];

  // 3. 构建并发查询任务
  for (let i = 0; i < batchTimes; i++) {
    tasks.push(
      db.collection(collectionName)
        .where(whereCondition)
        .orderBy('createdAt', 'asc')
        .skip(i * MAX_LIMIT)
        .limit(MAX_LIMIT)
        .get()
    );
  }

  // 4. 并发拉取并合并结果
  const results = await Promise.all(tasks);
  return results.reduce((acc, cur) => acc.concat(cur.data || []), []);
}
</file>

<file path="cloudfunctions/getArticles/package.json">
{
  "name": "getStories",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/getArticleSentences/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/getArticleSentences/dbHelper.js">
/**
 * 突破 100 条限制的分批拉取工具函数
 * @param {DB.Collection | DB.Query} query 数据库查询对象
 * @param {number} batchSize 每批次读取数量，默认 100
 * @returns {Promise<Array>} 合并后的完整数组
 */
async function fetchAll(query, batchSize = 100) {
  const countResult = await query.count();
  const total = countResult.total;

  if (total === 0) {
    return [];
  }

  const batchTimes = Math.ceil(total / batchSize);
  const tasks = [];

  for (let i = 0; i < batchTimes; i++) {
    const promise = query.skip(i * batchSize).limit(batchSize).get();
    tasks.push(promise);
  }

  const resArray = await Promise.all(tasks);
  return resArray.reduce((acc, cur) => acc.concat(cur.data), []);
}

/**
 * 统一成功响应
 */
function success(data, message = 'ok') {
  return { code: 0, data, message };
}

/**
 * 统一失败响应
 */
function fail(message = 'error', code = -1) {
  return { code, data: null, message };
}

module.exports = {
  fetchAll,
  success,
  fail
};
</file>

<file path="cloudfunctions/getArticleSentences/index.js">
// cloudfunctions/getSentences/index.js
const cloud = require('wx-server-sdk');
const { fetchAll, success, fail } = require('./dbHelper');

cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });
const db = cloud.database();

exports.main = async (event, context) => {
  try {
    const { articleId } = event;
    let query = db.collection('articleSentence');
    const whereCondition = {};

    if (articleId) whereCondition.articleId = articleId;
    // if (bookId) whereCondition.bookId = bookId;

    query = query.where(whereCondition).orderBy('index', 'asc');

    // 使用公共工具分批拉取全部句子数据
    const list = await fetchAll(query);

    return success(list);
  } catch (err) {
    console.error('[getArticleSentences] 失败:', err);
    return fail(err.message || '获取句子列表失败');
  }
};
</file>

<file path="cloudfunctions/getArticleSentences/package.json">
{
  "name": "getStorySentences",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/getBooks/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/getBooks/index.js">
// cloudfunctions/getBooks/index.js
const cloud = require('wx-server-sdk');
cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
});
const db = cloud.database();
const _ = db.command;

const MAX_LIMIT = 100;

exports.main = async (event) => {
  const {
    OPENID
  } = cloud.getWXContext();
  const {
    type
  } = event;

  try {
    // 1. 查出当前用户可见的所有书籍 (自己的 + 公共无 openid 的)
    const whereCondition = {
      openid: _.or([
        _.eq(OPENID),
        _.exists(false),
        _.eq(''),
        _.eq(null)
      ])
    };
    if (type) whereCondition.type = type;

    const books = await fetchAll('book', whereCondition);
    if (books.length === 0) {
      return {
        code: 0,
        msg: 'success',
        data: []
      };
    }

    // 2. 服务端按书籍类型并发统计数量
    const statTasks = books.map(async (book) => {
      let itemCount = 0;
      try {
        if (book.openid && book.openid === OPENID) {
          const countUserInput = await db.collection('sentence')
          .where({
            bookId: book._id
          })
          .count();

          const countUserRef = await db.collection('sentenceBookRef')
          .where({
            bookId: book._id
          })
          .count();
          itemCount = countUserInput.total + countUserRef.total || 0;

        } else if (book.type === 'article') {
          // 统计 article 集合的数量
          const countRes = await db.collection('article')
            .where({
              bookId: book._id
            })
            .count();
          itemCount = countRes.total || 0;
        } else {
          // 默认 / sentence：统计 sentence 集合的数量
          const countRes = await db.collection('sentence')
            .where({
              bookId: book._id
            })
            .count();
          itemCount = countRes.total || 0;
        }
      } catch (err) {
        console.error(`计算书籍 [${book.name || book._id}] 数量失败:`, err);
      }

      return {
        ...book,
        itemCount // 统一返回 itemCount 字段，供前端展示数量
      };
    });

    const booksWithCount = await Promise.all(statTasks);

    return {
      code: 0,
      msg: 'success',
      data: booksWithCount
    };

  } catch (err) {
    console.error('获取书籍列表失败:', err);
    return {
      code: 500,
      msg: '服务器异常',
      error: err
    };
  }
};

/**
 * 突破 100 条限制，全量并发拉取数据库记录
 */
async function fetchAll(collectionName, whereCondition) {
  const countRes = await db.collection(collectionName).where(whereCondition).count();
  const total = countRes.total;
  if (total === 0) return [];

  const batchTimes = Math.ceil(total / MAX_LIMIT);
  const tasks = [];

  for (let i = 0; i < batchTimes; i++) {
    tasks.push(
      db.collection(collectionName)
      .where(whereCondition)
      .orderBy('createdAt', 'desc')
      .skip(i * MAX_LIMIT)
      .limit(MAX_LIMIT)
      .get()
    );
  }

  const results = await Promise.all(tasks);
  return results.reduce((acc, cur) => acc.concat(cur.data || []), []);
}
</file>

<file path="cloudfunctions/getBooks/package.json">
{
  "name": "getBooks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/getBookSentence/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/getBookSentence/index.js">
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()
const _ = db.command

const MAX_LIMIT = 100 // 云数据库单次查询上限
const IN_CHUNK_SIZE = 20 // command.in() 单次数组长度上限，超过要分片查询
const STORAGE_PREFIX = 'cloud://cloud1-d0gyvvq93ab34b8b7.636c-cloud1-d0gyvvq93ab34b8b7-1333600691/'

exports.main = async (event) => {
  const { bookId } = event
  const { OPENID } = cloud.getWXContext()

  if (!bookId) {
    throw new Error('bookId 不能为空')
  }

  // 1. 该 bookId 下"直接归属"的句子（原来就有的逻辑，分页突破 100 条限制）
  const directSentences = await fetchAllByWhere('sentence', { bookId })

  // 2. 该 bookId 下"引用加入"的句子（用户从搜索里把别的句子加进了本词书）
  const refs = await fetchAllByWhere('sentenceBookRef', { bookId })
  const refSentenceIds = Array.from(new Set(refs.map(r => r.sentenceId)))

  const referencedSentences = refSentenceIds.length > 0
    ? await fetchByIdsIn('sentence', refSentenceIds)
    : []

  // 3. 合并去重（同一句子同时命中两边时，以直接归属的记录为准）
  const sentenceMap = new Map()
  directSentences.forEach(s => sentenceMap.set(s._id, s))
  referencedSentences.forEach(s => {
    if (!sentenceMap.has(s._id)) sentenceMap.set(s._id, s)
  })
  const rawSentences = Array.from(sentenceMap.values())

  // 4. 获取该用户对这些句子的全部 Mark 标记
  //    注意：这里按 sentenceId 查，不再按 bookId 查——
  //    一句话现在可能通过"引用"出现在多本词书里，
  //    收藏/学习进度是跟"用户 + 句子"绑定的，不应该被具体某本词书限制住。
  const sentenceIds = rawSentences.map(s => s._id)
  const marks = sentenceIds.length > 0
    ? await fetchMarksBySentenceIds(OPENID, sentenceIds)
    : []

  // ---------------- 💡 拼接相对路径 & 分片转换为 HTTPS 临时链接 ----------------

  const fileIds = []

  const toFullFileId = (path) => {
    if (!path) return ''
    if (path.startsWith('cloud://') || path.startsWith('http')) return path
    const cleanPath = path.startsWith('/') ? path.substring(1) : path
    return `${STORAGE_PREFIX}${cleanPath}`
  }

  rawSentences.forEach(s => {
    const fullAudio = toFullFileId(s.audio)
    const fullAudioZh = toFullFileId(s.audio_zh)

    if (fullAudio && fullAudio.startsWith('cloud://')) fileIds.push(fullAudio)
    if (fullAudioZh && fullAudioZh.startsWith('cloud://')) fileIds.push(fullAudioZh)
  })

  const urlMap = new Map()

  if (fileIds.length > 0) {
    const uniqueFileIds = Array.from(new Set(fileIds))
    const fileList = await batchGetTempFileURL(uniqueFileIds)

    fileList.forEach(item => {
      if (item.fileID && item.tempFileURL) {
        urlMap.set(item.fileID, item.tempFileURL)
      }
    })
  }

  // 5. 规范输出格式：填充 HTTPS 链接并映射成前端需要的对象
  const sentences = rawSentences.map(s => {
    const fullAudio = toFullFileId(s.audio)
    const fullAudioZh = toFullFileId(s.audio_zh)

    return {
      ...s,
      audio: urlMap.get(fullAudio) || fullAudio || s.audio,
      audio_zh: urlMap.get(fullAudioZh) || fullAudioZh || s.audio_zh
    }
  })

  return {
    code: 0,
    msg: 'success',
    data: {
      sentences,
      marks
    }
  }
}

/**
 * 分页拉取满足 where 条件的全部记录，突破单次 100 条限制
 */
async function fetchAllByWhere(collectionName, whereCondition) {
  const countRes = await db.collection(collectionName).where(whereCondition).count()
  const total = countRes.total
  if (total === 0) return []

  const batchTimes = Math.ceil(total / MAX_LIMIT)
  const tasks = []

  for (let i = 0; i < batchTimes; i++) {
    tasks.push(
      db.collection(collectionName)
        .where(whereCondition)
        .skip(i * MAX_LIMIT)
        .limit(MAX_LIMIT)
        .get()
    )
  }

  const results = await Promise.all(tasks)
  return results.reduce((acc, cur) => acc.concat(cur.data || []), [])
}

/**
 * 按 _id 批量查询（自动按 20 个一组分片，规避 command.in 的数量限制）
 */
async function fetchByIdsIn(collectionName, ids) {
  const chunks = []
  for (let i = 0; i < ids.length; i += IN_CHUNK_SIZE) {
    chunks.push(ids.slice(i, i + IN_CHUNK_SIZE))
  }

  const tasks = chunks.map(chunkIds =>
    db.collection(collectionName).where({ _id: _.in(chunkIds) }).get()
  )

  const results = await Promise.all(tasks)
  return results.reduce((acc, cur) => acc.concat(cur.data || []), [])
}

/**
 * 按用户 + 一批 sentenceId 拉取 mark（同样分片规避 command.in 限制）
 */
async function fetchMarksBySentenceIds(openid, sentenceIds) {
  const chunks = []
  for (let i = 0; i < sentenceIds.length; i += IN_CHUNK_SIZE) {
    chunks.push(sentenceIds.slice(i, i + IN_CHUNK_SIZE))
  }

  const tasks = chunks.map(chunkIds =>
    db.collection('sentenceMark')
      .where({ openid, sentenceId: _.in(chunkIds) })
      .get()
  )

  const results = await Promise.all(tasks)
  return results.reduce((acc, cur) => acc.concat(cur.data || []), [])
}

/**
 * 💡 分批获取临时 HTTPS 链接（规避 SDK 单次 50 个文件限制）
 */
async function batchGetTempFileURL(fileIds) {
  const CHUNK_SIZE = 50
  const tasks = []

  for (let i = 0; i < fileIds.length; i += CHUNK_SIZE) {
    const chunk = fileIds.slice(i, i + CHUNK_SIZE)
    tasks.push(cloud.getTempFileURL({ fileList: chunk }))
  }

  const results = await Promise.all(tasks)
  return results.reduce((acc, cur) => acc.concat(cur.fileList || []), [])
}
</file>

<file path="cloudfunctions/getBookSentence/package.json">
{
  "name": "getBookSentence",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/getFavoriteSentences/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/getFavoriteSentences/index.js">
const cloud = require('wx-server-sdk')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = cloud.database()
const _ = db.command
const MAX_LIMIT = 100
const STORAGE_PREFIX = 'cloud://cloud1-d0gyvvq93ab34b8b7.636c-cloud1-d0gyvvq93ab34b8b7-1333600691/'

// 补全 cloud:// 完整路径
function toFullFileId(path) {
  if (!path) return ''
  if (path.startsWith('cloud://') || path.startsWith('http')) return path
  const cleanPath = path.startsWith('/') ? path.substring(1) : path
  return `${STORAGE_PREFIX}${cleanPath}`
}

// 分批将 cloud:// 文件 ID 批量转成临时 HTTPS URL（限制每批 50 条）
async function getAudioUrls(list, batchSize = 50) {
  const ids = [...new Set(
    list.flatMap(x => [x.audio, x.audio_zh])
      .filter(x => typeof x === 'string' && x.startsWith('cloud://'))
  )]

  if (!ids.length) return list

  // 分批切片
  const chunks = []
  for (let i = 0; i < ids.length; i += batchSize) {
    chunks.push(ids.slice(i, i + batchSize))
  }

  // 并发转换所有批次
  const resArray = await Promise.all(
    chunks.map(fileList => cloud.getTempFileURL({ fileList }))
  )

  // 建立全局映射表
  const map = new Map()
  resArray.forEach(res => {
    (res.fileList || []).forEach(x => {
      if (x.status === 0) {
        map.set(x.fileID, x.tempFileURL)
      }
    })
  })

  // 重新映射返回数据
  return list.map(x => ({
    ...x,
    audio: map.get(x.audio) || x.audio || '',
    audio_zh: map.get(x.audio_zh) || x.audio_zh || ''
  }))
}

exports.main = async event => {
  const { OPENID } = cloud.getWXContext()
  const {
    bookId,
    cursor = null, // { lastCreatedAt, lastId }
    targetCount = 20
  } = event

  if (!bookId) {
    return {
      code: -1,
      msg: '缺少必要的 bookId 参数',
      data: null
    }
  }

  const count = Math.min(Math.max(Number(targetCount) || 20, 1), MAX_LIMIT)

  try {
    // 1. 构造 sentenceFavorite 的查询条件
    let where = {
      openid: OPENID,
      bookId: bookId
    }

    // 游标分页
    if (typeof cursor?.lastCreatedAt === 'number' && cursor.lastId) {
      where = _.and([
        where,
        _.or([
          { createdAt: _.lt(cursor.lastCreatedAt) },
          {
            createdAt: _.eq(cursor.lastCreatedAt),
            _id: _.lt(cursor.lastId)
          }
        ])
      ])
    }

    // 2. 查询收藏关联表（多取 1 条判断 hasMore）
    const favRes = await db.collection('sentenceFavorite')
      .where(where)
      .orderBy('createdAt', 'desc')
      .orderBy('_id', 'desc')
      .limit(Math.min(count + 1, MAX_LIMIT))
      .get()

    const favList = favRes.data || []
    const hasMore = favList.length > count
    const validFavs = favList.slice(0, count)

    if (!validFavs.length) {
      return {
        code: 0,
        msg: 'success',
        data: {
          list: [],
          nextCursor: cursor,
          hasMore: false
        }
      }
    }

    // 3. 提取 sentenceId 数组并批量拉取 sentence 物理表数据
    const sentenceIds = [...new Set(validFavs.map(x => x.sentenceId).filter(Boolean))]
    
    const [sentenceRes, markRes] = await Promise.all([
      sentenceIds.length ? db.collection('sentence').where({
        _id: _.in(sentenceIds)
      }).limit(MAX_LIMIT).get() : { data: [] },

      sentenceIds.length ? db.collection('sentenceMark').where({
        openid: OPENID,
        bookId: bookId,
        sentenceId: _.in(sentenceIds)
      }).limit(MAX_LIMIT).get() : { data: [] }
    ])

    const sentenceMap = new Map((sentenceRes.data || []).map(x => [x._id, x]))
    const markMap = new Map((markRes.data || []).map(x => [x.sentenceId, x]))

    // 4. 组装数据并保持顺序
    let rawList = validFavs.map(fav => {
      const sentence = sentenceMap.get(fav.sentenceId)
      if (!sentence) {
        console.warn('sentenceFavorite 对应的 sentence 不存在:', fav.sentenceId)
        return null
      }

      const mark = markMap.get(fav.sentenceId) || null

      return {
        ...sentence,
        favCreatedAt: fav.createdAt,
        mark,
        isFavorite: true
      }
    }).filter(Boolean)

    // 5. 补全路径并安全分批转换临时 URL
    rawList = rawList.map(item => ({
      ...item,
      audio: toFullFileId(item.audio),
      audio_zh: toFullFileId(item.audio_zh)
    }))

    const finalReadList = await getAudioUrls(rawList, 50)

    // 6. 计算下一页游标
    const lastFav = validFavs[validFavs.length - 1]
    const nextCursor = {
      lastCreatedAt: lastFav.createdAt || 0,
      lastId: lastFav._id
    }

    return {
      code: 0,
      msg: 'success',
      data: {
        list: finalReadList,
        nextCursor,
        hasMore
      }
    }

  } catch (err) {
    console.error('getFavoriteSentences 云函数异常:', err)

    return {
      code: -1,
      msg: err.message || '数据库查询或云函数异常',
      error: String(err)
    }
  }
}
</file>

<file path="cloudfunctions/getFavoriteSentences/package.json">
{
  "name": "getFavoriteSentences",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/getOpenId/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/getOpenId/index.js">
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })

exports.main = async () => {
  const { OPENID } = cloud.getWXContext()
  return { openid: OPENID }
}
</file>

<file path="cloudfunctions/getOpenId/package.json">
{
  "name": "getOpenId",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~4.0.2"
  }
}
</file>

<file path="cloudfunctions/getSentencePlayList/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/getSentencePlayList/index.js">
const cloud = require('wx-server-sdk')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = cloud.database()
const _ = db.command
const MAX_LIMIT = 100
const STORAGE_PREFIX = 'cloud://cloud1-d0gyvvq93ab34b8b7.636c-cloud1-d0gyvvq93ab34b8b7-1333600691/'

// 分批获取，突破云数据库单次 100 条限制
async function fetchAll(query, batchSize = 100) {
  const result = []
  let skip = 0

  while (true) {
    const res = await query.skip(skip).limit(batchSize).get()
    const data = res.data || []
    result.push(...data)
    if (data.length < batchSize) break
    skip += batchSize
  }

  return result
}

// 获取到期复习句子
async function getReviewList(openid, bookId, cursor, targetCount) {
  const reviewCursor = cursor?.reviewCursor || null
  let where = {
    openid,
    bookId,
    nextReviewAt: _.lte(Date.now())
  }

  // 游标分页：时间相同再用 _id 保证唯一顺序
  if (typeof reviewCursor?.lastNextReviewAt === 'number' && reviewCursor.lastId) {
    where = _.and([
      where,
      _.or([{
          nextReviewAt: _.gt(reviewCursor.lastNextReviewAt)
        },
        {
          nextReviewAt: _.eq(reviewCursor.lastNextReviewAt),
          _id: _.gt(reviewCursor.lastId)
        }
      ])
    ])
  }

  // 多取 1 条，用于判断是否还有更多
  const res = await db.collection('sentenceMark')
    .where(where)
    .orderBy('nextReviewAt', 'asc')
    .orderBy('_id', 'asc')
    .limit(Math.min(targetCount + 1, MAX_LIMIT))
    .get()

  const marks = res.data || []
  const hasMore = marks.length > targetCount
  const validMarks = marks.slice(0, targetCount)

  if (!validMarks.length) {
    return {
      list: [],
      nextCursor: reviewCursor,
      hasMore
    }
  }

  // 根据 sentenceId 批量获取句子
  const ids = validMarks.map(x => x.sentenceId).filter(Boolean)
  const sentenceRes = ids.length ?
    await db.collection('sentence').where({
      _id: _.in(ids)
    }).limit(MAX_LIMIT).get() : {
      data: []
    }

  const sentenceMap = new Map((sentenceRes.data || []).map(x => [x._id, x]))

  const list = validMarks.map(mark => {
    const sentence = sentenceMap.get(mark.sentenceId)

    if (!sentence) {
      console.warn('sentenceMark 对应的 sentence 不存在:', mark.sentenceId)
      return null
    }

    return {
      ...sentence,
      mark,
      isNew: false
    }
  }).filter(Boolean)

  // 注意：cursor 使用实际返回的最后一条，而不是多取的第 N+1 条
  const last = validMarks[validMarks.length - 1]

  return {
    list,
    nextCursor: {
      lastNextReviewAt: last.nextReviewAt,
      lastId: last._id
    },
    hasMore
  }
}

// 获取新句子，用于不足 targetCount 时补齐
async function getNewSentenceList(openid, bookId, cursor, targetCount) {
  const newCursor = cursor?.newSentenceCursor || null

  if (targetCount <= 0) {
    return {
      list: [],
      nextCursor: newCursor,
      hasMore: false
    }
  }

  // 获取用户已经学习过的句子，避免重复返回
  const marks = await fetchAll(
    db.collection('sentenceMark')
    .where({
      openid,
      bookId
    })
    .field({
      sentenceId: true
    })
  )

  const learnedIds = marks.map(x => x.sentenceId).filter(Boolean)
  let where = {
    bookId
  }

  if (learnedIds.length) where._id = _.nin(learnedIds)

  // 新句游标分页：createdAt + _id
  if (typeof newCursor?.lastCreatedAt === 'number' && newCursor.lastId) {
    where = _.and([
      where,
      _.or([{
          createdAt: _.gt(newCursor.lastCreatedAt)
        },
        {
          createdAt: _.eq(newCursor.lastCreatedAt),
          _id: _.gt(newCursor.lastId)
        }
      ])
    ])
  }

  // 多取 1 条，用于判断是否还有更多
  const res = await db.collection('sentence')
    .where(where)
    .orderBy('createdAt', 'asc')
    .orderBy('_id', 'asc')
    .limit(Math.min(targetCount + 1, MAX_LIMIT))
    .get()

  const raw = res.data || []
  const hasMore = raw.length > targetCount
  const sentences = raw.slice(0, targetCount)

  if (!sentences.length) {
    return {
      list: [],
      nextCursor: newCursor,
      hasMore
    }
  }

  const last = sentences[sentences.length - 1]

  return {
    list: sentences.map(x => ({
      ...x,
      mark: null,
      isNew: true
    })),
    nextCursor: {
      lastCreatedAt: last.createdAt || 0,
      lastId: last._id
    },
    hasMore
  }
}

// 将 cloud:// 文件 ID 转成临时 URL
async function getAudioUrls(list) {
  const ids = [...new Set(
    list.flatMap(x => [x.audio, x.audio_zh])
    .filter(x => typeof x === 'string' && x.startsWith('cloud://'))
  )]

  if (!ids.length) return list

  const res = await cloud.getTempFileURL({
    fileList: ids
  })
  const map = new Map(
    (res.fileList || [])
    .filter(x => x.status === 0)
    .map(x => [x.fileID, x.tempFileURL])
  )

  return list.map(x => ({
    ...x,
    audio: map.get(x.audio) || x.audio || '',
    audio_zh: map.get(x.audio_zh) || x.audio_zh || ''
  }))
}

function toFullFileId(path) {
  if (!path) return ''
  if (path.startsWith('cloud://') || path.startsWith('http')) return path
  const cleanPath = path.startsWith('/') ? path.substring(1) : path
  return `${STORAGE_PREFIX}${cleanPath}`
}


// 主流程：复习优先，不足部分用新句补齐
exports.main = async event => {
  const {
    OPENID
  } = cloud.getWXContext()
  const {
    bookId,
    cursor = null,
    targetCount = 20
  } = event

  if (!bookId) {
    return {
      code: -1,
      msg: '缺少必要的 bookId 参数',
      data: null
    }
  }

  const count = Math.min(Math.max(Number(targetCount) || 20, 1), MAX_LIMIT)

  try {
    // 先取复习句
    const review = await getReviewList(OPENID, bookId, cursor, count)

    // 复习不足时，用新句补齐
    const need = count - review.list.length
    const fresh = await getNewSentenceList(OPENID, bookId, cursor, need)

    // 合并后先统一文件路径，再转换临时 URL
    let list = [...review.list, ...fresh.list].map(item => ({
      ...item,
      audio: toFullFileId(item.audio),
      audio_zh: toFullFileId(item.audio_zh)
    }))
    // 合并并转换音频 URL
    list = await getAudioUrls(list)

    return {
      code: 0,
      msg: 'success',
      data: {
        list,
        nextCursor: {
          reviewCursor: review.nextCursor,
          newSentenceCursor: fresh.nextCursor
        },
        hasMore: review.hasMore || fresh.hasMore
      }
    }
  } catch (err) {
    console.error('getPlayList 云函数异常:', err)

    return {
      code: -1,
      msg: err.message || '数据库查询或云函数异常',
      error: String(err)
    }
  }
}
</file>

<file path="cloudfunctions/getSentencePlayList/package.json">
{
  "name": "getPlayList",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/renameField/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/renameField/index.js">
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

exports.main = async (event, context) => {
  const { collectionName, oldKey, newKey } = event;

  if (!collectionName || !oldKey || !newKey) {
    return { success: false, message: '缺少必要参数' };
  }

  const db = cloud.database();
  const _ = db.command;

  let totalUpdated = 0;
  let hasMore = true;

  try {
    while (hasMore) {
      // 1. 查询所有还包含 oldKey 的记录（单次批次处理 100 条）
      const queryRes = await db.collection(collectionName)
        .where({
          [oldKey]: _.exists(true)
        })
        .limit(100)
        .get();

      const list = queryRes.data;

      // 没有符合条件的数据，退出循环
      if (!list || list.length === 0) {
        hasMore = false;
        break;
      }

      // 2. 逐条处理：设置新字段，删除旧字段
      for (const item of list) {
        const oldValue = item[oldKey];

        await db.collection(collectionName)
          .doc(item._id)
          .update({
            data: {
              [newKey]: oldValue,    // 建立新字段并赋值
              [oldKey]: _.remove()   // 删除旧字段
            }
          });

        totalUpdated++;
      }
    }

    return {
      success: true,
      totalUpdated,
      message: `处理完成，共重命名 ${totalUpdated} 条记录`
    };
  } catch (err) {
    console.error('更新失败:', err);
    return {
      success: false,
      totalUpdated,
      error: err.errMsg || err.message
    };
  }
};
</file>

<file path="cloudfunctions/renameField/package.json">
{
  "name": "renameField",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/searchSentences/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/searchSentences/index.js">
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()
const _ = db.command

const MAX_RESULTS = 30
const MAX_VISIBLE_BOOKS = 200 // 简化处理：假设用户可见词书数量不会大到需要再分页
const IN_CHUNK_SIZE = 20 // command.in() 单次数组长度上限，超过要分片查询

function escapeRegExp(str) {
  return String(str).replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}

function chunk(arr, size) {
  const out = []
  for (let i = 0; i < arr.length; i += size) {
    out.push(arr.slice(i, i + size))
  }
  return out
}

/**
 * 跨词书搜索句子（中文或英文，模糊、大小写不敏感）
 *
 * 注意：只在"当前用户可见的词书"范围内搜索——
 * 即自己的词书 + 公共词书，不会搜到别人的私有词书内容，
 * 逻辑和 getBooks 云函数保持一致。
 *
 * MVP 版本用的是正则匹配，数据量大了以后建议换成
 * 云开发的全文检索能力或者自建分词索引。
 */
exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext()
  const keyword = ((event && event.keyword) || '').trim()

  if (!keyword) {
    return { code: 0, msg: 'success', data: [] }
  }

  try {
    const bookRes = await db.collection('book')
      .where({
        openid: _.or([_.eq(OPENID), _.exists(false), _.eq(''), _.eq(null)])
      })
      .limit(MAX_VISIBLE_BOOKS)
      .get()

    const bookMap = new Map()
    bookRes.data.forEach(b => bookMap.set(b._id, b.name))

    const visibleBookIds = Array.from(bookMap.keys())
    if (visibleBookIds.length === 0) {
      return { code: 0, msg: 'success', data: [] }
    }

    const re = db.RegExp({ regexp: escapeRegExp(keyword), options: 'i' })

    const bookIdChunks = chunk(visibleBookIds, IN_CHUNK_SIZE)
    const tasks = bookIdChunks.map(ids =>
      db.collection('sentence')
        .where(
          _.and([
            { bookId: _.in(ids) },
            _.or([{ en: re }, { zh: re }])
          ])
        )
        .limit(MAX_RESULTS)
        .get()
    )

    const results = await Promise.all(tasks)
    const merged = results.reduce((acc, cur) => acc.concat(cur.data || []), [])

    const data = merged
      .slice(0, MAX_RESULTS)
      .map(s => ({ ...s, bookName: bookMap.get(s.bookId) || '' }))

    return { code: 0, msg: 'success', data }
  } catch (err) {
    console.error('[searchSentences] 搜索失败:', err)
    return { code: 500, msg: '搜索失败', error: err }
  }
}
</file>

<file path="cloudfunctions/searchSentences/package.json">
{
  "name": "searchSentences",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~4.0.2"
  }
}
</file>

<file path="cloudfunctions/translate/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/translate/index.js">
const cloud = require('wx-server-sdk')
const https = require('https')
const crypto = require('crypto')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

function isChinese(text) {
  return /[\u4e00-\u9fff]/.test(text)
}

function md5(str) {
  return crypto.createHash('md5').update(str, 'utf8').digest('hex')
}

// 百度翻译 通用文本翻译API
// 文档：https://fanyi-api.baidu.com/doc/21
function translate(text, from, to) {
  return new Promise((resolve, reject) => {
    const appid = process.env.BAIDU_TRANSLATE_APPID
    const secretKey = process.env.BAIDU_TRANSLATE_SECRET_KEY

    if (!appid || !secretKey) {
      reject(new Error('未配置百度翻译密钥，请在云函数环境变量里设置 BAIDU_TRANSLATE_APPID 和 BAIDU_TRANSLATE_SECRET_KEY'))
      return
    }

    const salt = Date.now().toString()
    const sign = md5(appid + text + salt + secretKey)

    const params = new URLSearchParams({
      q: text,
      from,
      to,
      appid,
      salt,
      sign
    })

    const url = `https://fanyi-api.baidu.com/api/trans/vip/translate?${params}`

    https.get(url, res => {
      let data = ''

      res.on('data', chunk => {
        data += chunk
      })

      res.on('end', () => {
        try {
          const result = JSON.parse(data)

          if (result.error_code) {
            reject(new Error(`百度翻译失败[${result.error_code}]: ${result.error_msg || '未知错误'}`))
            return
          }

          const translated = (result.trans_result || []).map(r => r.dst).join('\n')
          resolve(translated)
        } catch (err) {
          reject(err)
        }
      })
    }).on('error', reject)
  })
}

exports.main = async (event) => {
  const text = (event.text || '').trim()

  if (!text) {
    throw new Error('请输入要翻译的内容')
  }

  const chinese = isChinese(text)

  // 百度翻译语言代码：中文是 zh，不是 zh-CN
  const source = chinese ? 'zh' : 'en'
  const target = chinese ? 'en' : 'zh'

  console.log('source:', source)
  console.log('target:', target)
  console.log('text:', text)

  const translatedText = await translate(text, source, target)

  return {
    source,
    target,
    original: text,
    translated: translatedText
  }
}
</file>

<file path="cloudfunctions/translate/package.json">
{
  "name": "translate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="cloudfunctions/upsertMark/config.json">
{
  "permissions": {
    "openapi": [
    ]
  }
}
</file>

<file path="cloudfunctions/upsertMark/index.js">
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

exports.main = async (event, context) => {
  const { OPENID } = cloud.getWXContext()
  const { sentenceId, bookId = '', patch = {} } = event

  if (!sentenceId) {
    return { success: false, error: 'sentenceId 不能为空' }
  }

  const collection = db.collection('sentenceMark')
  const id = `${OPENID}__${sentenceId}`
  const now = Date.now()

  try {
    // 先查询当前记录是否存在，以保持默认字段的完整性（仅在首次创建时填入默认值）
    // 或者利用 update 的结果结合 set
    const updateRes = await collection.doc(id).update({
      data: {
        ...patch,
        updatedAt: now
      }
    })

    // 如果更新成功，直接返回
    if (updateRes.stats && updateRes.stats.updated > 0) {
      return { success: true, id, action: 'update' }
    }
  } catch (err) {
    // 如果报错是因为 doc 不存在，捕获异常后走新建逻辑，不要直接 throw 挂掉
  }

  // 记录不存在，执行 set 创建，保证默认值全量写入
  await collection.doc(id).set({
    data: {
      _id: id,
      sentenceId,
      bookId,
      openid: OPENID,
      favorite: false,
      stage: 0,
      nextReviewAt: 0,
      createdAt: now,
      updatedAt: now,
      ...patch // 传入的 patch 会覆盖上面的默认值
    }
  })

  return { success: true, id, action: 'create' }
}
</file>

<file path="cloudfunctions/upsertMark/package.json">
{
  "name": "upsertMark",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "latest"
  }
}
</file>

<file path="miniprogram/enums/app-enums.ts">
// export enum LocalStorageKey {
//   progress_sequence = 'progress',
//   Progress_favorite = 'favorite',
//   progress_test = 'progress_test'
// }

export enum BookType {
  sentence = 'sentence',
  article = 'article'
}

export enum Pages {
  index = "/pages/index/index"
}

export enum cloudFunctionName {
  edgeTts = 'edgeTts',
  getOpenId = 'getOpenId',
  getStats = 'getStats',
  translate = 'translate',
  upsertMark = 'upsertMark',
  getBooks = 'getBooks',
  getBookSentence = 'getBookSentence',
  getArticles = 'getArticles',
  getArticleSentences = 'getArticleSentences',
  createBook = 'createBook',
  searchSentences = 'searchSentences',
  addSentenceToBook = 'addSentenceToBook',
  ensureUserBook = 'ensureUserBook',
  createSentence = 'createSentence',
  getSentencePlayList = 'getSentencePlayList',
  getFavoriteSentences = 'getFavoriteSentences'
}
</file>

<file path="miniprogram/pages/index/index.json">
{
  "usingComponents": {}
}
</file>

<file path="miniprogram/pages/index/index.ts">
import { callCloudFunction } from "../../services/cloud-client";

// pages/index/index.ts
Page({

  /**
   * 页面的初始数据
   */
  data: {

  },

  /**
   * 生命周期函数--监听页面加载
   */
  onLoad() {
    this.testGetFavoriteSentences()
  },

  async testGetFavoriteSentences() {
    try {
      const res = await callCloudFunction('getFavoriteSentences', {
        bookId: 'fav_oLhr065b5ezFkXzJtwt4gqi5jc_0', // 或传入你 ensureUserBook 返回的 favoriteBookId
        targetCount: 40
      });
      
      console.log('--- 收藏句子列表 ---', res.list);
      console.log('--- 下页游标 ---', res.nextCursor);
      console.log('--- 是否还有更多 ---', res.hasMore);
    } catch (err) {
      console.error('测试失败:', err);
    }
  },

  /**
   * 生命周期函数--监听页面初次渲染完成
   */
  onReady() {

  },

  /**
   * 生命周期函数--监听页面显示
   */
  onShow() {

  },

  /**
   * 生命周期函数--监听页面隐藏
   */
  onHide() {

  },

  /**
   * 生命周期函数--监听页面卸载
   */
  onUnload() {

  },

  /**
   * 页面相关事件处理函数--监听用户下拉动作
   */
  onPullDownRefresh() {

  },

  /**
   * 页面上拉触底事件的处理函数
   */
  onReachBottom() {

  },

  /**
   * 用户点击右上角分享
   */
  onShareAppMessage() {

  }
})
</file>

<file path="miniprogram/pages/index/index.wxml">
<!--pages/index/index.wxml-->
<text>pages/index/index.wxml</text>
</file>

<file path="miniprogram/pages/index/index.wxss">
/* pages/index/index.wxss */
</file>

<file path="miniprogram/services/article-service.ts">
import { cloudFunctionName } from "../enums/app-enums";
import { callCloudFunction } from "./cloud-client";

/**
 * 获取文章/课文列表
 */
export async function getArticles(bookId: string): Promise<Article[]> {
  return callCloudFunction<Article[]>(cloudFunctionName.getArticles, { bookId })
}

export async function getArticleSentences(articleId: string): Promise<ArticleSentence[]> {
  return callCloudFunction<ArticleSentence[]>(cloudFunctionName.getArticleSentences, { articleId })
}
</file>

<file path="miniprogram/services/book-service.ts">
// 1. services/book-service.ts (纯粹的数据服务)

import { BookType, cloudFunctionName } from "../enums/app-enums";
import { callCloudFunction } from "./cloud-client";

export class BookService {
  private booksCache: Book[] | null = null;

  async getBooks(): Promise<Book[]> {
    if (this.booksCache === null) {
      this.booksCache = await callCloudFunction(cloudFunctionName.getBooks,{});
      console.log(this.booksCache)
    }
    return this.booksCache || [];
  }

  async getSentenceBooks(){
    const books = await this.getBooks()
    return books.filter(item => item.type === BookType.sentence);
  }

  async getArticleBooks(){
    const books = await this.getBooks()
    return books.filter(item => item.type === BookType.article);
  }

  async getUserBooks(){
    const books = await this.getBooks()
    const openid = getApp().globalData.openid
    return books.filter(item => item.openid === openid);
  }

  /**
   * 创建词书。
   */
  async createBook(name: string, type: string): Promise<Book> {
    const book = await callCloudFunction(cloudFunctionName.createBook, { name, type })
    this.booksCache?.push(book)
    return book;
  }

  /**
   * 把一个已存在的句子加入某本词书（不复制内容，只建立引用关系）。
   * 用于"搜索句子 -> 点收藏 -> 选词书"这个场景。
   */
  async addSentenceToBook(
    sentenceId: string,
    bookId: string
  ): Promise<{ refId: string; alreadyIn?: boolean }> {
    return callCloudFunction(cloudFunctionName.addSentenceToBook, { sentenceId, bookId })
  }
}
const bookService = new BookService();
export default bookService;
</file>

<file path="miniprogram/services/cloud-client.ts">
// miniprogram/services/cloud-client.ts

export interface ApiResponse<T = any> {
  code: number
  data?: T
  msg?: string
}

/**
 * 统一调用云函数的封装
 */
export async function callCloudFunction<T = any>(
  name: string,
  data: Record<string, any> = {}
): Promise<T> {
  try {
    const res = await wx.cloud.callFunction({
      name,
      data
    })

    const result = res.result as ApiResponse<T>

    // 如果接口定义了 code 状态码
    if (result && typeof result.code === 'number') {
      if (result.code !== 0) {
        throw new Error(result.msg || `Cloud function ${name} error`)
      }
      return result.data as T
    }

    // 若无 code 直接返回 raw result
    return res.result as T
  } catch (err: any) {
    console.error(`[Cloud Call Error] ${name}:`, err)
    wx.showToast({
      title: err.message || '网络或服务异常',
      icon: 'none'
    })
    throw err
  }
}
</file>

<file path="miniprogram/services/sentence-service.ts">
import { cloudFunctionName } from "../enums/app-enums";
import { callCloudFunction } from "./cloud-client";

export class SentenceService {

  async getPlayList(bookId: string, targetCount = 20, cursor: PlayListCursor | null = null): Promise<PlayListResult> {
    const data = { bookId, targetCount, cursor }
    return callCloudFunction(cloudFunctionName.getSentencePlayList, data)
  }

  /**
   * 提交标记/阶段更新
   */
  static async updateMark(sentenceId: string, bookId: string, patch: Partial<SentenceMark>): Promise<SentenceMark> {
    const data = { sentenceId, bookId, patch }
    return callCloudFunction(cloudFunctionName.upsertMark, data)
  }

  /**
   * 录入一条新句子。
   * - 不管传入的 bookId 是哪本词书，句子物理内容都会被存进用户的默认库"用户句子"；
   * - 如果 bookId 不是默认库本身，云函数会在同一个事务里顺手建一条引用，
   *   让句子"看起来"出现在你选的那本词书里；
   * - 两步在同一个事务里，不会出现存了内容但引用没建上的半吊子状态。
   */
  static async createSentence(data: Sentence): Promise<string> {
    const res = await callCloudFunction<{ sentenceId: string }>(
      cloudFunctionName.createSentence,
      data
    )
    return res.sentenceId
  }

  /**
   * 跨词书搜索句子（中文或英文，模糊匹配）
   */
  static async searchSentences(keyword: string): Promise<Sentence[]> {
    return callCloudFunction(cloudFunctionName.searchSentences, { keyword })
  }

  /**
   * 获取指定词书/收藏夹中的句子列表
   * @param bookId 词书ID（可以是系统收藏夹 fav_${openid}，也可以是自建词书ID）
   * @param page 页码（可选，用于分页）
   * @param pageSize 每页条数（可选，默认 20）
   */
  async getFavoriteSentences(bookId: string, page = 1, pageSize = 20): Promise<Sentence[]> {
    return callCloudFunction<Sentence[]>(
      cloudFunctionName.getFavoriteSentences, // 确保你的 enum 中定义了对应的云函数名
      { bookId, page, pageSize }
    );
  }

  /**
   * @param sentenceId 句子ID
   * @param patch 待更新的字段
   * @param bookId 词书ID（选填）
   */
  async upsertMark(
    sentenceId: string,
    patch: Partial<SentenceMark> = {},
    bookId: string = ''
  ) {
    if (!sentenceId) {
      throw new Error('sentenceId 不能为空');
    }

    const db = wx.cloud.database();
    const collection = db.collection('sentenceMark');

    // 小程序端使用 set 时，云数据库安全性机制会自动在写入时绑定当前用户的 openid
    // 但 ID 规则仍与云函数保持一致：${openid}__${sentenceId}
    // 注：在前端 SDK 中，可通过 wx.cloud.database() 提供的全局机制或通过本地存储获取 openid，
    // 如果没有全局 openid，可以直接使用 docId 格式，或查询已有记录。

    const now = Date.now();

    try {
      // 1. 先尝试 update（最轻量，只更新传入的 patch 字段和 updatedAt）
      // 注意：在前端 SDK 中，如果记录不存在，update 会抛出 404 异常
      // 我们可以用 collection.where({ sentenceId }).get() 或直接 update 捕获

      // 简练高效的做法：先查一次旧记录，保证 `id` 格式或保持字段默认值
      // 如果你的 sentenceMark 在前端可以根据 _id 直接定位：
      // 因为 _id 依赖 OPENID，前端没有 OPENID 时，推荐按 (sentenceId + bookId) 查询或使用 doc 规则

      const queryRes = await collection.where({ sentenceId }).get();

      if (queryRes.data && queryRes.data.length > 0) {
        // 记录已存在 -> 执行更新
        const docId = queryRes.data[0]._id as string;
        await collection.doc(docId).update({
          data: {
            ...patch,
            updatedAt: now
          }
        });

        return { success: true, id: docId, action: 'update' };
      } else {
        // 记录不存在 -> 执行创建
        // 不传 _id 时云数据库会自动生成，也可以指定自定义 _id
        const newDoc = {
          sentenceId,
          bookId,
          favorite: false,
          stage: 0,
          nextReviewAt: 0,
          createdAt: now,
          updatedAt: now,
          ...patch
        };

        const addRes = await collection.add({
          data: newDoc
        });

        return { success: true, id: addRes._id, action: 'create' };
      }
    } catch (err: any) {
      console.error('[upsertMark Error]:', err);
      throw err;
    }
  }

  /**
   * 切换收藏状态
   */
  async toggleFavorite(
    sentenceId: string,
    bookId: string
  ): Promise<ToggleFavoriteResult> {
    const db = wx.cloud.database();
    const collection = db.collection('sentenceFavorite');

    // 拼装防重主键 ID: ${bookId}__${sentenceId}
    const docId = `${bookId}__${sentenceId}`;
    const docRef = collection.doc(docId);

    try {
      // 1. 查询该记录是否存在
      const res = await docRef.get().catch(() => null);

      if (res && res.data) {
        // 2. 存在 -> 删掉 (取消收藏)
        await docRef.remove();

        return {
          isFavorite: false,
          sentenceId,
          bookId
        };
      } else {
        // 3. 不存在 -> 插入 (添加收藏)
        // 在前端直接写入时，云数据库会自动把当前用户的 openid 加到 doc 属性中
        await collection.add({
          data: {
            _id: docId,
            bookId,
            sentenceId,
            createdAt: Date.now()
          }
        });

        return {
          isFavorite: true,
          sentenceId,
          bookId
        };
      }
    } catch (err: any) {
      console.error('[toggleFavorite Error]:', err);
      wx.showToast({
        title: '操作收藏失败',
        icon: 'none'
      });
      throw err;
    }
  }
}
</file>

<file path="miniprogram/services/translate-service.ts">
import { cloudFunctionName } from "../enums/app-enums"
import { callCloudFunction } from "./cloud-client"

export class TranslateService {
  static async translate(text: string): Promise<{
    zh: string
    en: string
  }> {
    const res = await callCloudFunction(
      cloudFunctionName.translate,
      { text }
    )

    const result = res as {
      source: string
      target: string
      original: string
      translated: string
    }

    const isChinese = result.source === 'zh'

    return {
      zh: isChinese ? result.original : result.translated,
      en: isChinese ? result.translated : result.original
    }
  }
}
</file>

<file path="miniprogram/services/tts-service.ts">
import { cloudFunctionName } from "../enums/app-enums"
import { callCloudFunction } from "./cloud-client"

export class TtsService {
  static async synthesize(text: string): Promise<TtsResult> {
    const res = await callCloudFunction(
      cloudFunctionName.edgeTts,
      { text }
    )

    const {
      fileID,
      wordBoundaries
    } = res as {
      fileID: string
      wordBoundaries: WordBoundary[]
    }

    const { fileList } = await wx.cloud.getTempFileURL({
      fileList: [fileID]
    })

    return {
      fileID,
      url: fileList[0].tempFileURL,
      wordBoundaries: wordBoundaries || []
    }
  }

  static async moveToPermanent(
    fileID: string,
    cloudPath: string
  ): Promise<string> {
    const { tempFilePath } =
      await wx.cloud.downloadFile({ fileID })

    const result = await wx.cloud.uploadFile({
      cloudPath,
      filePath: tempFilePath
    })

    await this.deleteTemp(fileID)

    return result.fileID
  }

  static async deleteTemp(fileID: string) {
    if (!fileID) return

    try {
      await wx.cloud.deleteFile({
        fileList: [fileID]
      })
    } catch (err) {
      console.warn('清理临时音频失败', err)
    }
  }

  static async deleteTemps(fileIDs: string[]) {
    const list = fileIDs.filter(Boolean)
    if (!list.length) return

    try {
      await wx.cloud.deleteFile({
        fileList: list
      })
    } catch (err) {
      console.warn('清理未保存音频失败', err)
    }
  }
}
</file>

<file path="miniprogram/utils/util.ts">
export const formatTime = (date: Date) => {
  const year = date.getFullYear()
  const month = date.getMonth() + 1
  const day = date.getDate()
  const hour = date.getHours()
  const minute = date.getMinutes()
  const second = date.getSeconds()

  return (
    [year, month, day].map(formatNumber).join('/') +
    ' ' +
    [hour, minute, second].map(formatNumber).join(':')
  )
}

const formatNumber = (n: number) => {
  const s = n.toString()
  return s[1] ? s : '0' + s
}
</file>

<file path="miniprogram/app.json">
{
  "pages": [
    "pages/index/index"
  ],
  "window": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "Weixin",
    "navigationBarBackgroundColor": "#ffffff"
  },
  "style": "v2",
  "componentFramework": "glass-easel",
  "lazyCodeLoading": "requiredComponents"
}
</file>

<file path="miniprogram/app.ts">
import { cloudFunctionName } from "./enums/app-enums"
import { callCloudFunction } from "./services/cloud-client"

// app.ts
App<IAppOption>({
  globalData: {
  },
  async onLaunch() {
    // 初始化云环境
    wx.cloud.init({
      env: 'cloud1-d0gyvvq93ab34b8b7',
      traceUser: true
    })
    // 运行期间屏幕亮
    wx.setKeepScreenOn({
      keepScreenOn: true
    })

    const cached = wx.getStorageSync('openid')
    if (cached) {
      this.globalData.openid = cached
    } else {
      try {
        const res = await wx.cloud.callFunction({ name: 'getOpenId' })
        const openid = (res.result as { openid: string }).openid
        this.globalData.openid = openid
        wx.setStorageSync('openid', openid)
      } catch (e) {
        console.error('获取 openid 失败', e)
      }
    }

    // 确保用户的默认词书（"用户句子"）存在。
    // 云函数内部是幂等的，这里不用 await 阻塞启动，失败了也不影响正常使用——
    // 后续录入句子等操作里如果发现默认库缺失，会再次兜底触发。
    this.ensureUserBook()
  },

  async ensureUserBook() {
    // 直接拿到 callCloudFunction 解包后的对象
    const userBooks = await callCloudFunction<UserDefaultBooks>(
      cloudFunctionName.ensureUserBook,
      {}
    );

    console.log('默认词书信息:', userBooks);
    console.log('录入词书ID:', userBooks.originBookId);
    console.log('收藏词书ID:', userBooks.favoriteBookId);

    // 缓存到 globalData 或 Storage
    this.globalData.originBookId = userBooks.originBookId;
    this.globalData.favoriteBookId = userBooks.favoriteBookId;
  },

  getPlayConfig() {
    if (!this.globalData.playConfig) {
      this.globalData.playConfig = {
        repeatCount: 1,
        gapMs: 1000,
        playOrder: 'zh_first',
        limitCount: 0,
        bookId: '',
        bookName: '',
        playMode: 'sequence',
        startId: ''
      }
    }

    return this.globalData.playConfig
  },

  updatePlayConfig(config) {
    Object.assign(this.getPlayConfig(), config)
  }

})
</file>

<file path="miniprogram/app.wxss">
/**app.wxss**/
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  padding: 200rpx 0;
  box-sizing: border-box;
}
</file>

<file path="package.json">
{
  "name": "miniprogram-ts-quickstart",
  "version": "1.0.0",
  "description": "",
  "scripts": {
  },
  "keywords": [],
  "author": "",
  "license": "",
  "dependencies": {
  },
  "devDependencies": {
    "miniprogram-api-typings": "^2.8.3-1"
  }
}
</file>

<file path="project.config.json">
{
  "description": "项目配置文件",
  "miniprogramRoot": "miniprogram/",
  "compileType": "miniprogram",
  "cloudfunctionRoot": "cloudfunctions/",
  "setting": {
    "useCompilerPlugins": [
      "typescript"
    ],
    "babelSetting": {
      "ignore": [],
      "disablePlugins": [],
      "outputPath": ""
    },
    "coverView": false,
    "postcss": false,
    "minified": false,
    "enhance": false,
    "showShadowRootInWxmlPanel": false,
    "packNpmRelationList": [],
    "ignoreUploadUnusedFiles": true,
    "compileHotReLoad": false,
    "skylineRenderEnable": true,
    "es6": false,
    "uglifyFileName": false,
    "uploadWithSourceMap": true,
    "packNpmManually": false,
    "minifyWXSS": true,
    "localPlugins": false,
    "condition": false,
    "compileWorklet": false,
    "disableUseStrict": false,
    "minifyWXML": true
  },
  "simulatorType": "wechat",
  "simulatorPluginLibVersion": {},
  "condition": {},
  "srcMiniprogramRoot": "miniprogram/",
  "editorSetting": {
    "tabIndent": "insertSpaces",
    "tabSize": 2
  },
  "libVersion": "trial",
  "packOptions": {
    "ignore": [],
    "include": []
  },
  "appid": "wx638218468cec82b2"
}
</file>

<file path="project.private.config.json">
{
  "description": "项目私有配置文件。此文件中的内容将覆盖 project.config.json 中的相同字段。项目的改动优先同步到此文件中。详见文档：https://developers.weixin.qq.com/miniprogram/dev/devtools/projectconfig.html",
  "projectname": "english_listening",
  "setting": {
    "compileHotReLoad": true,
    "urlCheck": true,
    "coverView": false,
    "lazyloadPlaceholderEnable": false,
    "skylineRenderEnable": true,
    "preloadBackgroundData": false,
    "autoAudits": false,
    "showShadowRootInWxmlPanel": false,
    "showES6CompileOption": false,
    "useGlassEaselForWxml": false,
    "bigPackageSizeSupport": false,
    "useApiHook": true,
    "checkInvalidKey": true,
    "useStaticServer": false,
    "useLanDebug": false,
    "ignoreDevUnusedFiles": true
  },
  "libVersion": "3.17.3"
}
</file>

<file path="tsconfig.json">
{
  "compilerOptions": {
    "strictNullChecks": true,
    "noImplicitAny": true,
    "module": "CommonJS",
    "target": "ES2020",
    "allowJs": true,
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "experimentalDecorators": true,
    "noImplicitThis": true,
    "noImplicitReturns": true,
    "alwaysStrict": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "strict": true,
    "strictPropertyInitialization": true,
    "lib": ["ES2020"],
    "typeRoots": [
      "./typings"
    ]
  },
  "include": [
    "./**/*.ts"
  ],
  "exclude": [
    "node_modules"
  ]
}
</file>

</files>
