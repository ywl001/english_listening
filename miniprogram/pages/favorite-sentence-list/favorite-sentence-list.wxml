<view class="container">

  <!-- 搜索框 -->
  <view class="search-bar">
    <input class="search-input" placeholder="搜索收藏的句子" bindinput="onSearchInput" value="{{keyword}}" confirm-type="search" />
  </view>

  <!-- 骨架屏 -->
  <view wx:if="{{loading}}" class="skeleton-list">
    <view wx:for="{{[0,1,2,3,4]}}" wx:key="*this" class="skeleton-card">
      <view class="skeleton-line skeleton-en"></view>
      <view class="skeleton-line skeleton-zh"></view>
    </view>
  </view>

  <!-- 列表 -->
  <view class="list-wrapper">
    <block wx:if="{{displayList.length > 0}}">
      <view wx:for="{{displayList}}" wx:key="_id" class="sentence-card {{currentIndex === index && isPlayingAll ? 'playing-card' : ''}}" bindtap="playSingleAudio" data-index="{{index}}">
        <view class="card-left">
          <view class="en-text">
            <text class="index-num">{{index + 1}}. </text>{{item.en || item.english}}
          </view>
          <view class="zh-text">{{item.zh || item.chinese}}</view>
        </view>

        <!-- 收藏心形 -->
        <view class="favorite-btn" catchtap="onDelete" data-id="{{item._id || item.id}}" data-index="{{index}}">
          <view class="heart"></view>
        </view>
      </view>

      <view class="loading-status"></view>
    </block>

    <view wx:else class="empty-box">
      <text>暂无相关句子</text>
    </view>
  </view>

  <!-- 可拖动的悬浮添加按钮 -->
  <view class="fab-add" style="left: {{fabX}}px; top: {{fabY}}px;" catchtouchstart="onFabTouchStart" catchtouchmove="onFabTouchMove" catchtouchend="onFabTouchEnd">
    <text class="fab-icon">+</text>
  </view>

  <!-- 底部播放按钮 -->
  <view class="bottom-floating-bar" wx:if="{{sentenceList.length > 0}}">
    <button class="play-all-btn" bindtap="togglePlayAll">
      <text class="icon">🎧</text>
      <text>{{isPlayingAll ? '暂停播放' : '全部播放收藏 (' + sentenceList.length + ')'}}</text>
    </button>
  </view>
</view>